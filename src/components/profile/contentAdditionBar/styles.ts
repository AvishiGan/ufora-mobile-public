import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    iconAndTextContainer: {
        display: "flex",
        padding: 10,
        alignItems: "center",
        gap: 10,
        alignSelf: "stretch",
        borderRadius: 15,
        background: "#FEFEFE",
        flexDirection: "row",
        paddingBottom: 15,
    },
});