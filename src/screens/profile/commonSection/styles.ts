import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    topSectionContainer: {
        flexShrink: 0,
        flexDirection: "column",
        alignItems: "center",
        display: "flex",
    },
});