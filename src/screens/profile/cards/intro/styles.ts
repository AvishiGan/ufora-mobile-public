import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    seeMore: {
        // color: var(--brand-blue, #2656FF);
        color: '#2656FF',
        leadingTrim: 'both',
        textEdge: 'cap',
        // fontFamily: 'Poppins',
        fontSize: 14,
        fontStyle: 'normal',
        fontWeight: '700',
        lineHeight: 16,
    }
});